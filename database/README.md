# Database

This folder contains all files related to the SQLite database for the Hockey Data Fun project, including the schema, scripts to populate data, and scripts to update the database over time.

## Folder Contents

- `tables_ddl.sql` - SQL file with all CREATE TABLE statements for the database.
- `population_scripts/` - Python scripts to populate and update the database (players, teams, games, etc.).

